generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime?
  image         String?
  userProfilePK Int?      @unique
  accounts      Account[]
  sessions      Session[]
  importantEvents ImportantEvent[]
  bodyBatteries BodyBattery[]
  respirations  Respiration[]
  stresses      Stress[]
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

enum EventImpact {
  POSITIVE
  NEGATIVE
}

enum EventVisibility {
  PUBLISHED
  NOT_PUBLIC
  PRIVATE
}

model ImportantEvent {
  id             String           @id @default(cuid())
  userProfilePK  Int
  title          String
  name           String
  slug           String           @unique
  publishDate    DateTime         @db.Date @default(now())
  tags           String[]
  expectedEffect EventImpact
  visibility     EventVisibility  @default(NOT_PUBLIC)
  descriptionHtml String
  startDate      DateTime    @db.Date
  endDate        DateTime    @db.Date
  categories     Category[]
  user           User        @relation(fields: [userProfilePK], references: [userProfilePK], onDelete: Cascade)
  createdAt      DateTime    @default(now())
  updatedAt      DateTime    @updatedAt

  @@index([userProfilePK, startDate, endDate])
  @@index([visibility])
  @@index([publishDate])
  @@map("important_event")
}

model Category {
  id        String           @id @default(cuid())
  name      String
  slug      String           @unique
  events    ImportantEvent[]
  createdAt DateTime         @default(now())
  updatedAt DateTime         @updatedAt

  @@map("category")
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model BodyBattery {
  pk_date                      DateTime @db.Date
  userProfilePK                Int
  chargedValue                 Int?
  drainedValue                 Int?
  highest_statTimestamp        DateTime?
  highest_statsValue           Int?
  lowest_statTimestamp         DateTime?
  lowest_statsValue            Int?
  sleepend_statTimestamp       DateTime?
  sleepend_statsValue          Int?
  sleepstart_statTimestamp     DateTime?
  sleepstart_statsValue        Int?
  user                         User     @relation(fields: [userProfilePK], references: [userProfilePK], onDelete: Cascade)

  @@id([pk_date, userProfilePK])
  @@index([pk_date])
  @@map("body_battery")
}

model Respiration {
  pk_date                   DateTime @db.Date
  userProfilePK             Int
  avgWakingRespirationValue Float?
  highestRespirationValue  Float?
  lowestRespirationValue   Float?
  user                      User     @relation(fields: [userProfilePK], references: [userProfilePK], onDelete: Cascade)

  @@id([pk_date, userProfilePK])
  @@index([pk_date])
  @@map("respiration")
}

model Stress {
  pk_date                             DateTime @db.Date
  userProfilePK                       Int
  awake_averageStressLevel            Float?
  awake_averageStressLevelIntensity   Float?
  awake_highDuration                  Int?
  awake_lowDuration                   Int?
  awake_maxStressLevel                Int?
  awake_mediumDuration                Int?
  awake_restDuration                  Int?
  awake_stressDuration                Int?
  awake_stressIntensityCount          Int?
  awake_totalDuration                 Int?
  awake_totalStressCount              Int?
  awake_totalStressIntensity          Int?
  user                                User     @relation(fields: [userProfilePK], references: [userProfilePK], onDelete: Cascade)

  @@id([pk_date, userProfilePK])
  @@index([pk_date])
  @@map("sress")
}
